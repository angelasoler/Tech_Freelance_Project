<!DOCTYPE html>
<html>
  <head>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <div class="container-fluid">
        <a class="navbar-brand" href="/">Tech Freelancers</a>
        <div class="collapse navbar-collapse">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <% if signed_in? %>
              <% if owner_signed_in? %>
                <li class="nav-item">
                  <a class="nav-link disabled"><%= current_owner.try(:email) %></a>
                </li>
                <li class="nav-item">
                  <a class="nav-link"><%= link_to 'Logout', destroy_owner_session_path, method: :delete %></a>
                </li>
              <% else %>
                <li class="nav-item">
                  <a class="nav-link disabled"><%= current_freelancer.try(:email) %></a>
                </li>
                <li class="nav-item">
                  <a class="nav-link"><%= link_to 'Logout', destroy_freelancer_session_path, method: :delete %></a>
                </li>
              <% end %>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>
    <title>TechFreelancersProject</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>   
  </head>


  <body>
    <div class="container">
      <div class="row">
        <div class="col-12">
          <div class="my-3">
            <%= yield %>
          </div>
        </div>
      </div>
    </div>
  </body>

  <p class="notice alert-primary container-fluid"><%= notice %></p>
  <p class="alertalert-primary container-fluid"><%= alert %></p>
</html>
